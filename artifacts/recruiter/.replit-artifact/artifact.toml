kind = "web"
previewPath = "/"
title = "AI Recruiter"
version = "1.0.0"
id = "artifacts/recruiter"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 21156

[services.development]
run = "pnpm --filter @workspace/recruiter run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/recruiter", "run", "build" ]
publicDir = "artifacts/recruiter/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "21156"
BASE_PATH = "/"
